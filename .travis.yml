# 构建所需的语言环境
language: node_js
  node_js:
    - "v12.16.0"
# 构建的分支
branches:
  only:
    - develop
# 设置时区
before_install:
    - export TZ='Asia/Shanghai'
# 安装编译工具
install:
  - npm install -g gitbook-cli
  - gitbook install
script:
  - gitbook build
deploy:
  provider: pages
  skip-cleanup: true
  # github 上的token环境变量
  github-token: f2edf8080828b6ebefcabc28bc0a1c2015a97b01 
  # 根据情况自定义到静态文件输出目录
  local-dir: ./_book/ 
  target-branch: gh-pages
  verbose: true
